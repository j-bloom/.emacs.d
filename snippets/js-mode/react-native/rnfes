# -*- mode: snippet -*-
# name: React Native Functional Component with Stylesheet
# key: rnfes
# --
import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ${1:index} = () => {
  return (
    <View>
      <Text>${1:index}</Text>
    </View>
  )
}

export default ${1:index}

const styles = StyleSheet.create({})